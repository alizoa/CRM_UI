// src/hooks/useNotificationUnreadCount.ts — demo mode

export function useNotificationUnreadCount() {
  return {
    notificationCount: 2,
    decrement: () => {},
    clear: () => {},
    refresh: () => {},
  };
}
